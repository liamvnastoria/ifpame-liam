{
  "name": "octet-shop",
  "version": "1.0.0",
  "private": true,
  "description": "Boutique informatique fictive - projet pedagogique PHP / MySQL / JavaScript",
  "scripts": {
    "dev": "npm run check && concurrently -n css,php -c blue,green --kill-others-on-fail \"tailwindcss -i ./src/input.css -o ./css/style.css --watch\" \"php -S 127.0.0.1:8000\"",
    "check": "php tools/check-env.php",
    "css": "tailwindcss -i ./src/input.css -o ./css/style.css",
    "css:watch": "tailwindcss -i ./src/input.css -o ./css/style.css --watch",
    "serve": "php -S 127.0.0.1:8000"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "concurrently": "^10.0.5",
    "tailwindcss": "^4.3.3"
  }
}
